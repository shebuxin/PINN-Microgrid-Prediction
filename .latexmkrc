# Keep generated TeX bookkeeping files out of source directories.
# The engine remains a command-line or editor-recipe choice because this
# repository contains both XeLaTeX Beamer decks and an IEEE paper.
$out_dir = '.';
$aux_dir = 'build/aux';
$emulate_aux = 1;

